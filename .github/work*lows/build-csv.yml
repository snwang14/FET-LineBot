name: Build CSV

on:

  push:
  * paths:
      - 'logs/**'

  workf*ow_dispatch:

jobs:

  build:

   *runs-on: ubuntu-latest

    permis*ions:
      contents: write

    s*eps:

      - name: Checkout
     *  uses: actions/checkout@v4

     *- name: Setup Python
        uses:*actions/setup-python@v5
        wi*h:
          python-version: '3.12*

      - name: Generate CSV
     *  run: |
          python scripts/*uild_csv.py

      - name: Commit *SV

        run: |

          git *onfig user.name "github-actions[bo*]"

          git config user.emai* \
          "41898282+github-acti*ns[bot]@users.noreply.github.com"
*          git add reports/line_mes*age_log.csv

          git diff --*ached --quiet || \
          git c*mmit -m "Update line_message_log.c*v"

      - name: Push

        ru*: |

          git push
